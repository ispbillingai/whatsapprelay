# Android app (kept locally, not in repo)
app/
gradle/
.gradle/
.idea/
build.gradle
settings.gradle
gradle.properties
gradlew
gradlew.bat
keystore.jks
local.properties
SETUP.md
txt.txt

# Server folder (files are at repo root instead)
server/

# Server config (has DB credentials - differs per environment)
config.php

# OS files
.DS_Store
Thumbs.db
