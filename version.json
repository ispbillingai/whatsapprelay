{
    "versionCode": 42,
    "versionName": "2.5.7",
    "apkUrl": "https://whatsapp.ispledger.com/whatsapp.apk",
    "changelog": "v2.5.7: Fix the 'works for a day then needs reboot' bug — sendAttempted static flag was getting stuck at true after PollingService's outer timeout fired, blocking every subsequent send's retry loop from starting. Now reset properly at both the timeout and the start of every send."
}
